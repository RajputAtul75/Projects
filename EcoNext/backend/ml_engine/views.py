# Visual search is served by products.api_views.visual_search, which uses the
# lazy singleton from ml_engine.visual_search.  This file is kept as a
# placeholder so the ml_engine app has a views module.
